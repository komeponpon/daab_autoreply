//module.exports = (robot) => {
  //robot.hear(/.*/, (res) => {
    //トークルームのIDを確認
    //res.send(`This room id is ${res.message.room}`);
  //});

  //デバッグコンソールへ組織IDを表示
  //robot.hear(/.*/, () => {
    //const messageDomain = res.message.rooms[res.message.room].domain.id;
    //console.log(messageDomain);
  //});
//}